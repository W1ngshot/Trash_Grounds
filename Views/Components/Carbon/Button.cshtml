@{
    var buttonClass = $"{ViewBag.ClassName} bx--btn bx--btn--{ViewBag.Type}";
    if (ViewBag.Size == "small")
        buttonClass += " bx--btn--sm";
}

<button class="@buttonClass" type="button">
    @ViewBag.Text
</button>